const element=document.getElementById("my_element") as HTMLInputElement;
element.value="Hello world";

// the as keyword strongly defines the element as HTMLInputElement.
// as keyword is used for type assertion 
